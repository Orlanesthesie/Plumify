<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password | Plumify</title>
    <link rel="icon" href="{{ asset('images/logos/icon.png') }}">

    {% block stylesheets %}
    <!-- Bootstrap CSS CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
    {% endblock %}
</head>
<body>
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

<div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="login-card card p-4 shadow-lg">
            <div class="text-center">
            <a href="/">
                <img src="{{ asset('images/logos/logo1.png') }}" alt="Plumify Logo" class="logo mb-5" width="80">
            </a>
            </div>

            <div class="d-flex align-items-center justify-content-center mb-4">
                <a onclick="window.history.back()" class="mr-2 chevron">
                    <i class="fa-solid fa-chevron-left"></i>
                </a>
                <h2 class="mb-0">Forgot password ?</h2>
            </div>
        <div class="text-body-secondary mb-3">
             Please enter your email to reset the password.
        </div>
         <form action="{{ path('app_reset_password') }}" method="post">
                <div class="form-group">
                    <label for="username">Email:</label>
                    <input type="text" class="form-control" id="username" name="_username" required>
                </div>
                <button type="submit" class="btn btn-login btn-block"><strong>Reset</strong></button>
            </form>
        {# {{ form_start(form) }}
            {{ form_row(form.email) }}
            <button type="submit" class="btn btn-login btn-block mt-3"><strong>Reset password</strong></button>
        {{ form_end(form) }} #}
            <div class="register-link text-center mt-3">
                <p>New member? <a href="{{ path('app_registration') }}">Register here</a></p>
            </div>
        </div>
    </div>

   
</body>
</html>